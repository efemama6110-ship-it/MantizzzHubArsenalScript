-- Load the Rayfield library
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- Configuration Variables (Combat)
local aimbotEnabled = false
local rightClickHeld = false
local teamCheckEnabled = true
local fovCheckEnabled = false
local headshotEnabled = true
local triggerbotEnabled = false
local killAllEnabled = false
local killAllConfirmed = false
local fovRadiusValue = 100
local smoothnessValue = 1

-- Configuration Variables (Visuals)
local espOutlineEnabled = false
local espBoxEnabled = false
local espNameEnabled = false
local espHealthEnabled = false
local espDistanceEnabled = false
local espTracersEnabled = false
local espSkeletonEnabled = false

-- Configuration Variables (Movement)
local flyToggleEnabled = false
local isFlying = false
local flySpeed = 50
local speedToggleEnabled = false
local walkSpeedValue = 16
local noclipEnabled = false
local infJumpEnabled = false

-- Fly Controllers
local bodyGyro, bodyVelocity
local flyCtrl = {F = 0, B = 0, L = 0, R = 0}

-- Create the FOV Draw Circle
local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Thickness = 1
FOVCircle.NumSides = 64
FOVCircle.Radius = fovRadiusValue
FOVCircle.Filled = false
FOVCircle.Visible = false

-- ESP Drawing Cache
local espCache = {}

local function getESP(player)
    if not espCache[player] then
        espCache[player] = {
            Box = Drawing.new("Square"),
            Name = Drawing.new("Text"),
            HealthBarBG = Drawing.new("Square"),
            HealthBar = Drawing.new("Square"),
            Distance = Drawing.new("Text"),
            Tracer = Drawing.new("Line"),
            H2T = Drawing.new("Line"), 
            T2L = Drawing.new("Line"), 
            T2R = Drawing.new("Line"), 
            T2LL = Drawing.new("Line"), 
            T2RL = Drawing.new("Line")  
        }
        espCache[player].Box.Color = Color3.fromRGB(255, 0, 0)
        espCache[player].Box.Thickness = 1.5
        espCache[player].Box.Filled = false
        
        local texts = {espCache[player].Name, espCache[player].Distance}
        for _, txt in pairs(texts) do
            txt.Color = Color3.fromRGB(255, 255, 255)
            txt.Size = 13
            txt.Center = true
            txt.Outline = true
            txt.Font = 2
        end
        espCache[player].Tracer.Color = Color3.fromRGB(255, 0, 0)
        espCache[player].Tracer.Thickness = 1
        espCache[player].HealthBarBG.Color = Color3.fromRGB(0, 0, 0)
        espCache[player].HealthBarBG.Filled = true
        espCache[player].HealthBarBG.Thickness = 1
        espCache[player].HealthBar.Filled = true
        espCache[player].HealthBar.Thickness = 1
    end
    return espCache[player]
end

local function cleanPlayerESP(player)
    if espCache[player] then
        for _, obj in pairs(espCache[player]) do obj:Remove() end
        espCache[player] = nil
    end
end
Players.PlayerRemoving:Connect(cleanPlayerESP)

local function isOpponent(player)
    if player == LocalPlayer then return false end
    if teamCheckEnabled and player.Team and LocalPlayer.Team then
        if player.Team == LocalPlayer.Team and player.Team.Name ~= "FFA" then
            return false
        end
    end
    return true
end

-- Create the Rayfield Window
local Window = Rayfield:CreateWindow({
   Name = "MantizzzHub 🚀",
   LoadingTitle = "Loading Full Features...",
   LoadingSubtitle = "Arsenal Ready",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false,
})

---------------------------------------------------------
-- TABS
---------------------------------------------------------
local CombatTab = Window:CreateTab("Combat", 4483362458)
CombatTab:CreateToggle({Name = "Enable Aimbot (Hold Right Click)", CurrentValue = false, Callback = function(Value) aimbotEnabled = Value end })
CombatTab:CreateToggle({Name = "Target Headshot", CurrentValue = true, Callback = function(Value) headshotEnabled = Value end })
CombatTab:CreateToggle({Name = "Triggerbot (Teleport)", CurrentValue = false, Callback = function(Value) triggerbotEnabled = Value end })
CombatTab:CreateToggle({Name = "Team Check", CurrentValue = true, Callback = function(Value) teamCheckEnabled = Value end })
CombatTab:CreateSlider({Name = "FOV Radius", Range = {30, 500}, Increment = 5, CurrentValue = 100, Callback = function(Value) fovRadiusValue = Value end })
CombatTab:CreateSlider({Name = "Smoothness", Range = {1, 20}, Increment = 1, CurrentValue = 1, Callback = function(Value) smoothnessValue = Value end })

local VisualsTab = Window:CreateTab("Visuals", 7059346386)
VisualsTab:CreateToggle({Name = "Enemy Outline ESP", CurrentValue = false, Callback = function(Value) espOutlineEnabled = Value end })
VisualsTab:CreateToggle({Name = "Enemy Box ESP", CurrentValue = false, Callback = function(Value) espBoxEnabled = Value end })
VisualsTab:CreateToggle({Name = "Skeleton ESP", CurrentValue = false, Callback = function(Value) espSkeletonEnabled = Value end })
VisualsTab:CreateToggle({Name = "Tracers", CurrentValue = false, Callback = function(Value) espTracersEnabled = Value end })
VisualsTab:CreateToggle({Name = "Show Names", CurrentValue = false, Callback = function(Value) espNameEnabled = Value end })
VisualsTab:CreateToggle({Name = "Show Distance", CurrentValue = false, Callback = function(Value) espDistanceEnabled = Value end })
VisualsTab:CreateToggle({Name = "Show Health Bar", CurrentValue = false, Callback = function(Value) espHealthEnabled = Value end })

local MovementTab = Window:CreateTab("Movement")
MovementTab:CreateToggle({Name = "Enable Fly (Press 'X')", CurrentValue = false, Callback = function(Value) flyToggleEnabled = Value if not Value and isFlying then stopFly() end end })
MovementTab:CreateSlider({Name = "Fly Speed", Range = {10, 200}, Increment = 5, CurrentValue = 50, Callback = function(Value) flySpeed = Value end })
MovementTab:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(Value) noclipEnabled = Value end })
MovementTab:CreateToggle({Name = "Infinite Jump", CurrentValue = false, Callback = function(Value) infJumpEnabled = Value end })

---------------------------------------------------------
-- LOGIC
---------------------------------------------------------
function startFly()
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    bodyGyro = Instance.new("BodyGyro", hrp)
    bodyGyro.P = 9e4
    bodyGyro.maxTorque = Vector3.new(9e9, 9e9, 9e9)
    bodyGyro.cframe = hrp.CFrame
    bodyVelocity = Instance.new("BodyVelocity", hrp)
    bodyVelocity.velocity = Vector3.new(0, 0, 0)
    bodyVelocity.maxForce = Vector3.new(9e9, 9e9, 9e9)
    isFlying = true
end

function stopFly()
    isFlying = false
    if bodyGyro then bodyGyro:Destroy() end
    if bodyVelocity then bodyVelocity:Destroy() end
end

RunService.Stepped:Connect(function()
    if noclipEnabled and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed then
        if input.UserInputType == Enum.UserInputType.MouseButton2 then rightClickHeld = true end
        if input.KeyCode == Enum.KeyCode.X and flyToggleEnabled then
            if isFlying then stopFly() else startFly() end
        end
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then rightClickHeld = false end
end)

local function getPlayerClosestToCrosshair()
    local nearestCharacter = nil
    local shortestDist = math.huge
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    for _, player in pairs(Players:GetPlayers()) do
        if isOpponent(player) and player.Character then
            local targetPart = player.Character:FindFirstChild("Head") or player.Character:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                local dist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                if onScreen and dist <= fovRadiusValue and dist < shortestDist then
                    shortestDist = dist
                    nearestCharacter = player.Character
                end
            end
        end
    end
    return nearestCharacter
end

RunService.RenderStepped:Connect(function()
    -- FOV
    FOVCircle.Radius = fovRadiusValue
    FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    FOVCircle.Visible = aimbotEnabled and fovCheckEnabled
    
    -- Fly/Aimbot Logic
    if isFlying and bodyVelocity and bodyGyro then
        bodyGyro.cframe = Camera.CFrame
    end
    
    if aimbotEnabled and rightClickHeld then
        local target = getPlayerClosestToCrosshair()
        if target and target:FindFirstChild("Head") then
            local targetCFrame = CFrame.lookAt(Camera.CFrame.Position, target.Head.Position)
            Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, 1 / smoothnessValue)
        end
    end
    
    -- ESP Rendering Loop
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local char = player.Character
            local esp = getESP(player)
            local isAlive = char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Head") and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0
            if isAlive and isOpponent(player) then
                local rootPart = char.HumanoidRootPart
                local head = char.Head
                local bottomPos = Camera:WorldToViewportPoint(rootPart.Position - Vector3.new(0, 3, 0))
                local topPos = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 1, 0))
                if espBoxEnabled then esp.Box.Size = Vector2.new(100, 200) esp.Box.Position = Vector2.new(topPos.X, topPos.Y) esp.Box.Visible = true else esp.Box.Visible = false end
            else
                for _, obj in pairs(esp) do if typeof(obj) == "userdata" then obj.Visible = false end end
            end
        end
    end
end)
